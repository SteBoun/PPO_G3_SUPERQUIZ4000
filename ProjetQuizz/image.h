#ifndef IMAGE_H
#define IMAGE_H
#include <stdio.h>
class IMAGE_H {

public:

image();
//image();

private:

int m_definition;//variable contenant la définition de l'image
std::string m_nom ;//variable contenant le nom de l'image
std::string m_description;//variable contenant la description de l'image
};

#endif // IMAGE_H
