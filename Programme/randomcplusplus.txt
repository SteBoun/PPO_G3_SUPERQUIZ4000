#include <iostream>
#include <random>
#include <stdlib.h>
#include <stdio.h>
#include <vector>

std::vector<int> Tab_nbAleatoire;

int aleatoire(int min,int max,std::vector<int> &Tableau)
{
    std::random_device RAND_E;
    std::mt19937 random_engine(RAND_E());
    std::uniform_int_distribution<int> distrib{min,max};//prépare la zone de generation
    int nbAleatoire = distrib(RAND_E);
    std::cout << "Debug_Aleatoire = " << nbAleatoire << std::endl;//affiche un nombre aleatoire
    Tableau.push_back(nbAleatoire);
    return nbAleatoire;
}

int main()
{
    aleatoire(1,15,Tab_nbAleatoire);
    std::cout << Tab_nbAleatoire[0];
}